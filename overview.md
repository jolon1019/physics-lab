# 三个电学实验操作台优化（欧姆定律 / 电功率 / 电磁铁）

按用户要求，对 `e-ohm`、`e-power`、`e-magnet` 三个实验的操作台做了重新设计与美化，统一收敛到与电阻实验一致的新野兽派电路台风格。

## 改造前的问题
- 三个组件原仅画「抽象图 + 滑块」，没有正确连通的电路图，线路显示差（与之前电阻实验的反馈一致）。
- e-power 仅用两根裸横线表示导线，不是真实回路；e-magnet 只有粗陋线圈+大头针，无电路。

## 改造后（三处统一）
- **规整矩形回路**：电源 + 开关 + 滑动变阻器 + 负载（电阻/灯泡/螺线管）串联，导线沿矩形四边走、元件贴边、电子粒子沿回路流动（速度∝电流）。
- **实时读数** + **记录对比表**（空态提示 + 记录后自动滚动，最多 6 行，便于归纳控制变量法规律）。
- **响应式**：≤1180px 自动单列；电子流动、发光、吸引数目均随参数实时变化。

## 各实验亮点
- **欧姆定律 (OhmLab.vue)**：电路图（电压表并联测 U、电流表串联测 I）+ 控制变量模式（自由 / 探究 I–U 固定 R / 探究 I–R 固定 U，受控模式锁定无关滑块）+ **电路图 ↔ I–U / I–R 关系图** 一键切换。
- **电功率 (PowerLab.vue)**：正确电路图（电压表并联在灯泡两端）；灯泡亮度随实际功率 P=UI 变化（光晕+灯丝）；额定 U额=2.5V，状态判定「较暗 / 正常 / 过亮」。
- **电磁铁 (ElectromagnetLab.vue)**：螺线管绕在铁芯上，可切换「有/无铁芯」（×3 增强）；显示 N/S 磁极、磁场外弧线、电流方向；吸引大头针数随磁性（I×N×铁芯）变化，两端分布；匝数 50/100/150 可选。

## 验证
- 生产构建成功：`vite build` → 93 模块转换，ExperimentView chunk 282.80 KB，无错误。
- Vite 热转换：OhmLab / PowerLab / ElectromagnetLab 均 HTTP 200，无编译错误。
- dev 服务器运行在 5174 端口（5173 被用户自身占用）。

> 注：`.build-three-check` 为临时验证目录，受本机回收站安全钩子限制无法删除，未被应用导入、无害。
